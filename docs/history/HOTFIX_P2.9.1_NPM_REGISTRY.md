# Hotfix P2.9.1 — npm registry

Corrigido o `package-lock.json`, que continha 8 URLs de pacotes resolvidas por um registro interno do ambiente de desenvolvimento. Todas foram substituídas pelas URLs públicas equivalentes do npm (`https://registry.npmjs.org/`).

Nenhuma dependência, versão ou hash de integridade foi alterado.
