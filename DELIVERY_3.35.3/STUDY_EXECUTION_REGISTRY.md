# studyExecutionRegistry

O registro versionado declara capacidades reais de execução por disciplina e assunto. Para Língua Portuguesa, o notebook cadastrado é `DATAPREV 2026 — Língua Portuguesa — Tutor FGV`, com `READY_THEORY_ONLY`, `fgvEvidenceStatus = PENDING` e `fgvStyleTeaching = DISABLED`.

As únicas fontes carregadas e aprovadas são as 13 listadas em `REGISTRO_NOTEBOOKS.md`. A taxonomia continua sendo usada internamente, mas não é apresentada como fonte do NotebookLM.
